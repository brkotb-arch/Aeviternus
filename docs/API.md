# API

## Overview

Aeviternus exposes a REST interface used by both the web interface and external integrations.

Current endpoints include:

- chat
- memory
- upload
- image processing
- voice interaction
- runtime status

The API is intentionally lightweight while the primary system intelligence resides inside the Runtime Core.

Future versions will introduce:

- authentication
- versioned endpoints
- streaming responses
- runtime inspection